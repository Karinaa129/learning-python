# Блок 2: Условный оператор

## О блоке
В этом блоке я изучила, как заставить программу принимать решения в зависимости от условий. Это основа логики в программировании.

## Пройденные темы

### 4.1 Выбор из двух
- Оператор `if`
- Оператор `else`
- Операторы сравнения (`>`, `<`, `>=`, `<=`, `==`, `!=`)
- Цепочки сравнений (`1 < x < 10`)

### 4.2 Логические операции
- `and` (логическое И)
- `or` (логическое ИЛИ)
- `not` (логическое НЕ)
- Приоритет операторов

### 4.3 Вложенные и каскадные условия
- Вложенные `if` внутри других `if`
- Каскадные условия через `elif`

### Итоговая работа
- `tasks/final_part1.py`
- `tasks/final_part2.py`

## Файлы блока
- `01_if_else.py` — выбор из двух
- `02_logical_operations.py` — логические операции
- `03_nested_conditions.py` — вложенные и каскадные условия

## Статус
- [x] 4.1 Выбор из двух
- [x] 4.2 Логические операции
- [x] 4.3 Вложенные и каскадные условия
- [ ] Итоговая работа

## Итог
